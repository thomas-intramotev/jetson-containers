# setup.py
from setuptools import setup

setup(
    name='opencv-python',
    version='4.12.0',  # High version to prevent upgrades
    install_requires=[],
    description='Dummy package - opencv-contrib-python is installed instead'
)